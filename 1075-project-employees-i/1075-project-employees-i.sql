# Write your MySQL query statement below
SELECT P.PROJECT_ID, IFNULL(ROUND(SUM(E.EXPERIENCE_YEARS)/COUNT(E.EMPLOYEE_ID), 2), 0) AS average_years FROM PROJECT P LEFT JOIN EMPLOYEE E ON P.EMPLOYEE_ID = E.EMPLOYEE_ID group by project_id;