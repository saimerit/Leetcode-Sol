# Write your MySQL query statement below
SELECT R.CONTEST_ID, ROUND(COUNT(R.USER_ID) * 100/ (SELECT COUNT(*) FROM USERS), 2) AS percentage FROM REGISTER R LEFT JOIN USERS U ON U.USER_ID = R.USER_ID GROUP BY R.CONTEST_ID ORDER BY percentage DESC, R.CONTEST_ID ASC;